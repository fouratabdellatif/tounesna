<form method="post" action="{{ path('app_rate_hotel_delete', {'idRate': rate_hotel.idRate}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rate_hotel.idRate) }}">
    <button class="btn">Delete</button>
</form>
