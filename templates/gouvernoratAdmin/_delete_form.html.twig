<form method="post" action="{{ path('admin_gouvernorat_delete', {'idGouver': gouvernorat.idGouver}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gouvernorat.idGouver) }}">
    <button class="btn">Delete</button>
</form>
